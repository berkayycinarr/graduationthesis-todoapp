@{
    ViewBag.Title = "Anasayfa";
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <style>
        /* CSS Kodları Buraya Eklenecek */
        /* Genel Arka Plan */
        body {
            background: linear-gradient(to bottom, #FFC0CB, #800080, #000080); /* Pembe, mor ve lacivert renkler arasında geçiş */
            font-family: Arial, sans-serif;
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            display: grid;
            grid-template-rows: auto 1fr auto; /* Header, Main, Footer */
            min-height: 100vh;
            overflow-y: auto; /* Yalnızca dikey scroll bar görünür */
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #fff; /* Beyaz arka plan */
            color: #333; /* Metin rengi */
            padding: 10px 20px;
        }

        .logo {
            display: flex;
            align-items: center;
        }

        .logo img {
            width: 40px;
            height: 40px;
            margin-right: 10px;
        }

        .logo h1 {
            margin: 0;
            color: #000; /* Siyah renk */
        }

        /* Navbar Butonları */
        .auth-buttons button {
            background-color: #800080; /* Mor arka plan */
            color: #fff;
            border: none;
            padding: 12px 24px; /* Buton boyutunu artır */
            margin-left: 10px;
            cursor: pointer;
            border-radius: 20px; /* Butonların köşelerini yuvarla */
            transition: background-color 0.3s; /* Geçiş efekti */
        }

        .auth-buttons button:hover {
            background-color: rgba(255, 192, 203, 0.7); /* Açık pembeye dönüş */
        }


        /* Ana İçerik Kısmı */
        #main-content {
            display: flex; /* Main içeriğini flex olarak ayarladık */
            flex-direction: column; /* Dikey olarak düzenler */
            gap: 35px; /* Section'lar arasındaki mesafeyi belirliyoruz */
            padding: 20px;
        }

        /* Article */
        article {
            display: flex; /* Article içeriğini flex olarak ayarladık */
            flex-direction: column; /* Dikey olarak düzenler */
            gap: 20px; /* İçerikler arasındaki mesafeyi belirliyoruz */
        }

        /* İçerik Bölümleri */
        .welcome-section,
        .about-section,
        .motivation-section {
            background-color: #f5f5f5;
            padding: 20px;
            border-radius: 10px; /* Köşeleri yuvarla */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Hafif gölgelendirme */
        }

        /* Hoş Geldiniz Bölümü */
        .welcome-section {
            text-align: center; /* İçeriği ortala */
            margin-top: 6px;
        }

        /* Hoş Geldiniz Butonu */
        .welcome-section button.start-button {
            background-color: #800080;
            color: #fff;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 20px;
            transition: transform 0.3s ease; /* Buton büyüme efekti ve geçiş süresi */
        }

        .welcome-section button.start-button:hover {
            background-color: rgba(255, 192, 203, 0.7);
            transform: scale(1.1); /* Buton büyüklüğünü arttır */
        }

        /* Alıntı Bölümü */
        .motivation-section blockquote {
            border-left: 5px solid #000080; /* Lacivert renk */
            padding-left: 10px;
        }

        /* İçerik 2 ve 3 */
        .about-section,
        .motivation-section {
            flex: 1; /* İçeriklerin eşit genişlikte olmasını sağlar */
        }

        /* Container */
        .sections-container {
            display: flex;
            flex-direction: column; /* Dikey olarak düzenler */
            gap: 35px; /* İçerikler arasındaki mesafeyi belirliyoruz */
        }

        /* Footer */
        footer {
            background-color: #fff; /* Beyaz arka plan */
            color: #333; /* Metin rengi */
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <div class="logo">
            <a href="#"><img src="~/Homepage/icons8-note-48.png" alt="noteLogo"></a>
            <h1>TODO TADAA!</h1>
        </div>
        <div class="auth-buttons">
            <form asp-controller="Home" asp-action="Giriş" method="post">
            <button type="submit"  class="login-button">
                Giriş Yap
             </button>
            </form>

            <form asp-controller="Home" asp-action="KayıtOl" method="post">
            <button type="submit" class="register-button">
               Kayıt Ol
             </button>
            </form>
        </div>
    </nav>

    <!-- Ana İçerik Kısmı -->
    <main>
        <section class="welcome-section">
            <h2>Hoş Geldiniz!</h2>
            <p>Siteye hoş geldiniz. Notlarınızı almak ve organize etmek için hemen başlayın!</p>
            <button class="start-button">Buradan Başla</button>
        </section>

        <section class="about-section">
            <h2>Site Hakkında</h2>
            <p>Bu site, not alma, yapılacaklar listesi tutma ve takvim özelliklerini bir araya getiren bir platformdur. Kullanıcılar, notlarını kolayca oluşturabilir, düzenleyebilir ve takvim özelliği ile önemli tarihleri planlayabilirler.</p>
        </section>

        <section class="motivation-section">
            <h2>Motivasyon</h2>
            <blockquote id="quote">
                <p id="quote-text">"Kararlılık, disiplin ve sıkı çalışmayla tüm hayaller gerçeğe dönüşür."</p>
                <p id="quote-author">— Lailah Gifty Akita</p>
            </blockquote>
        </section>
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Ana Sayfa</p>
    </footer>
</body>
</html>

